// This file was auto-generated by Fern from our API Definition.

import type * as KardApi from "../../../index.js";

/**
 * A draft offer link. The offer is not yet linked to the experiment and some properties may be missing.
 */
export interface OfferLinkDraft extends KardApi.OfferLinkBase {
    /** The id of the offer that is linked to the experiment. */
    offerId?: string;
    /** The rules query that was used to create the test group segment. */
    rules?: unknown;
    /** The status of the offer link. */
    status: "DRAFT";
}
